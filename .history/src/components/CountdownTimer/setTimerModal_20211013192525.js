import React from 'react';
import ModalContainer from './modalStyles';

const SetTimerModal = () => {
    return (
        <ModalContainer>
            This is the Modal
        </ModalContainer>
    );
};

export default SetTimerModal;